<form action="/node/<%= host %>/<%= port %>/?secret=<%= password %>" method="post" target="_blank">
  <input type="hidden" name="password" value="<%= password %>">
  <button class="btn btn-primary" type="submit">
    <i class="fa fa-eye"></i> Connect to Pluto
  </button>
</form>
